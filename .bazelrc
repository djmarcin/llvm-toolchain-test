# To confirm the toolchain that was used.
build -s

build --incompatible_strict_action_env
build --incompatible_enable_cc_toolchain_resolution
build --extra_toolchains=@llvm_toolchain//:cc-toolchain-darwin
